# *************************************************
# 最初にすべての階層のファイルを無視する
# 階層は必要なだけ追加する
# *************************************************

/*
/.**

# *************************************************
# .gitignoreは上から解釈されていくので
# 管理対象をホワイトリストのように追記する
# .zfunc/** ディレクトリは管理したいけど中身はいらない場合
# *************************************************

!/README.md
!.gitignore
